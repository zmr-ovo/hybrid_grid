# Hybrid Grid 后续研究与实现计划

## 1. 目标与当前基础

本计划用于指导中期报告之后的代码实现和实验。后续工作围绕两条主线展开：

1. 在固定总参数预算下，利用跨 GOP 共享模型和 GOP 独立 LoRA 提高长视频表示效率。
2. 改进 Grid 熵模型，在尽量保持重建质量和推理效率的条件下降低整体码率，并完成真实码流验证。

当前已经具备以下基础：

- 未压缩论文模型可以从头训练、断点恢复和独立评测。
- 数据顺序、时空坐标、随机种子、训练统计和 FPS 评测流程已经稳定。
- 压缩分支已经实现 Grid 可微量化、基础熵模型、端到端率失真训练和整体估计 BPP。
- 非 Grid 参数已经支持差异化 QAT：二维 Linear 权重使用按输出通道的 8-bit QAT，一维参数和熵模型参数保持 FP32。
- GOP 分组、局部时间坐标、GOP0 共享模型、网络 LoRA、矩阵 Grid LoRA、结构化 Grid LoRA，以及单 GOP 全参数微调对照均已实现。
- 当前单 GOP 实验说明：只修改 Decoder 不足以适配后续 GOP；Grid 参数是影响跨 GOP 适配质量的主要部分；只在 GOP0 上训练并冻结共享模型，会让后续 LoRA 承担过多新内容。

## 2. 总体原则

### 2.1 保持已有实验入口独立

- `train.py` 继续作为未压缩基线入口。
- `train_compression.py` 继续作为压缩训练入口。
- `train_gop_upper_bound.py` 保留为单 GOP 全参数微调对照。
- `train_gop_grid_residual.py` 保留为单 GOP Grid 适配实验入口。
- 后续全 GOP 联合训练使用新的独立入口，不把联合训练逻辑混入已有脚本。

### 2.2 每次只验证一个研究问题

- 先验证全 GOP 联合训练能否改善共享模型，再研究 GOP rank 分配。
- GOP-LoRA 主线稳定后，再改进熵模型。
- 熵模型的概率估计有效后，再实现算术编码和真实 BPP。
- 在前一阶段验收前，不提前加入下一阶段的功能。

### 2.3 保持公平比较

- 主要实验使用相同数据、分辨率、训练轮数、随机种子和评测代码。
- GOP 方案与全视频基线比较时，统计共享模型和全部 GOP LoRA 的总参数量。
- 压缩实验同时报告重建质量、估计 BPP、真实 BPP、熵模型参数和必要元数据。
- 未完成真实算术编码前，所有码率统一标注为估计 BPP。

## 3. 阶段一 全 GOP 联合训练

### 3.1 研究目的

当前共享模型主要由 GOP0 训练得到，没有学习后续 GOP 中的共同信息。阶段一需要让一份共享模型在所有 GOP 上共同更新，同时保留每个后续 GOP 的独立 LoRA，使共享模型表示共性，LoRA 表示局部变化。

### 3.2 新增文件

```text
hybrid_grid/
├── train_gop_joint.py
├── gop_lora/
│   ├── joint.py
│   ├── model.py
│   ├── grid_residual.py
│   ├── injection.py
│   ├── linear.py
│   ├── partition.py
│   └── video_dataset.py
└── tests/
    ├── test_gop_joint_model.py
    └── test_train_gop_joint.py
```

`joint.py` 只保存联合训练需要的参数选择、GOP 调度和统计工具。模型主体仍复用现有 `GOPStructuredGridHybridGridNet`，避免复制网络前向过程。

### 3.3 第一版功能边界

第一版只支持已经完成单 GOP 验证的结构：

- 网络 LoRA 注入范围固定为 `all_linear`。
- Grid 适配固定为 `structured`。
- GOP0 不配置独立 LoRA，使用共享模型直接重建。
- GOP1 至最后一个 GOP 各自拥有独立的网络 LoRA 和结构化 Grid LoRA。
- 所有后续 GOP 第一版使用相同的 network rank 和 Grid rank。
- 暂不加入熵模型、QAT、自动 rank 分配和并行多卡训练。

### 3.4 训练流程

#### 步骤 A 共享模型初始化

- 从 GOP0 已训练 checkpoint 加载共享模型。
- 检查 GOP 划分、模型结构、Grid 层数、特征维度和时间尺度是否一致。
- 为所有后续 GOP 同时创建独立 LoRA。
- 输出共享模型、每个 GOP 网络 LoRA、每个 GOP Grid LoRA和总参数量。

#### 步骤 B 全 GOP 联合训练

- 每个 epoch 遍历全部 GOP。
- GOP0 前向时只使用共享模型。
- 后续 GOP 前向时使用共享模型及当前 GOP 对应的 LoRA。
- 每个 batch 只包含同一个 GOP，防止错误地混用 LoRA。
- 共享模型在所有 GOP 上更新，学习跨 GOP 的共同内容。
- 当前 GOP 只更新自己的网络 LoRA 和 Grid LoRA，不能修改其他 GOP 的 LoRA。
- 共享模型学习率低于 LoRA 学习率。

建议第一轮实验使用：

- 共享模型学习率：`1e-4` 或 `2e-4`。
- Grid LoRA 学习率：`5e-3`。
- 网络 LoRA 学习率：`1e-3`。
- warmup 比例：`0.05`。
- network rank：`8`，其中各层自动限制有效 rank。
- structured Grid rank：先使用单 GOP 实验中表现较好的 rank。

这些数值作为首轮实验起点，不写死在模型实现中。

#### 步骤 C 逐 GOP 微调

- 联合训练完成后冻结共享模型。
- 依次微调 GOP1 至最后一个 GOP 的 LoRA。
- 微调当前 GOP 时，其他 GOP LoRA 保持冻结。
- GOP0 只进行评测，不再单独更新。
- 保存联合训练 checkpoint 和最终微调 checkpoint，二者不能互相覆盖。

### 3.5 GOP 采样与损失汇总

- 第一版按 GOP 顺序循环训练，保证每个 GOP 每轮都被访问。
- 每个 GOP 内部保持帧级 shuffle。
- 全视频平均损失按实际帧数加权，最后一个较短 GOP 不应与完整 GOP 获得相同权重。
- 同时记录每个 GOP 的训练 PSNR 和全视频训练 PSNR。
- 验证阶段分别输出每个 GOP 与全视频的 PSNR、MS-SSIM。

### 3.6 Checkpoint 内容

checkpoint 至少保存：

- checkpoint 类型和版本。
- 共享模型参数。
- 全部 GOP 的网络 LoRA 和 Grid LoRA 参数。
- 优化器和学习率调度器状态。
- 当前训练阶段、epoch 和当前微调 GOP。
- 全视频最佳训练 PSNR 和最佳验证 PSNR。
- 各 GOP 最佳验证 PSNR。
- GOP 划分信息、模型配置、LoRA 配置和随机状态。

需要支持：

- 从联合训练阶段恢复。
- 从逐 GOP 微调阶段恢复。
- `eval-only` 评测联合 checkpoint 或最终 checkpoint。

### 3.7 日志输出

启动时输出：

- 视频帧数、GOP 长度和 GOP 数量。
- 共享模型参数量。
- 每个 GOP 的网络 LoRA 和 Grid LoRA 参数量。
- 全部 LoRA 参数量和模型总参数量。
- 各参数组的学习率。

每个 epoch 输出：

- 当前阶段和 epoch。
- 每个 GOP 的平均 loss、PSNR 和 MS-SSIM。
- 全视频加权平均 loss、PSNR 和 MS-SSIM。
- 共享模型、Grid LoRA 和网络 LoRA 的当前学习率。

验证时输出：

- 每个 GOP 的帧数、PSNR 和 MS-SSIM。
- 全视频 PSNR 和 MS-SSIM。
- 当前最佳结果及对应 epoch。

### 3.8 验收条件

- 一个命令可以完成联合训练和逐 GOP 微调。
- GOP1 至最后一个 GOP 均拥有独立 LoRA，共享模型只有一份。
- 训练某个 GOP 时不会更新其他 GOP 的 LoRA。
- 全视频指标按帧数正确汇总。
- 参数量统计无遗漏、无重复。
- 支持断点恢复和 `eval-only`。
- CPU 测试可以完成小模型前向和一个训练步骤。
- 固定随机种子后重复评测结果一致。
- 不影响现有 baseline、compression 和单 GOP 入口。

## 4. 阶段二 固定参数预算下的 GOP rank 分配

### 4.1 研究目的

在阶段一证明联合训练有效后，再研究如何将固定的 LoRA 参数预算分配给不同 GOP。目标不是单纯增加 rank，而是在与全视频基线相近的总参数量下提高重建质量。

### 4.2 实现内容

- 支持通过列表为每个 GOP 指定 Grid rank，例如 `--grid_ranks 6 6 6 2`。
- 支持通过列表指定 network rank；第一轮实验可继续保持相同 network rank。
- 根据实际 Grid 尺寸计算每个 GOP、每个层级的 LoRA 参数量。
- 在启动训练前检查总参数是否超过用户设置的预算。
- 增加只计算参数、不启动训练的 `--report_params_only` 模式。

### 4.3 第一轮分配策略

- 完整 GOP 使用相同的结构化 Grid rank。
- 最后一个帧数较少的 GOP 使用较小 rank。
- 网络 LoRA rank 先保持不变，避免同时改变过多因素。
- 根据阶段一各 GOP 的验证差距，再决定是否为运动或内容变化较大的 GOP 增加 rank。

### 4.4 后续可选策略

仅在固定 rank 实验完成后考虑：

- 按 GOP 帧数分配参数。
- 按共享模型初始重建误差分配参数。
- 按 GOP 内容变化强度分配参数。
- 逐层使用不同 Grid rank。

### 4.5 验收条件

- 指定 rank 后能准确报告每个 GOP 和整体参数量。
- 超过参数预算时在训练前报错。
- 相同配置的参数统计结果稳定一致。
- 完成与全视频基线相同或相近总参数量的全视频实验。
- 报告联合训练、逐 GOP 微调以及最终全视频结果，不能只报告训练集最佳值。

## 5. 阶段三 GOP 方案的实验与决策

### 5.1 必做对比

- 全视频未压缩基线。
- GOP0 共享模型加 Decoder LoRA。
- GOP0 共享模型加全部线性层 LoRA。
- 单 GOP 全参数微调对照。
- 完整 Grid 增量加网络 LoRA 对照。
- 矩阵 Grid LoRA 加网络 LoRA。
- 结构化 Grid LoRA 加网络 LoRA。
- 全 GOP 联合训练加结构化 Grid LoRA。

### 5.2 必做消融

- 共享模型只使用 GOP0 训练，与全 GOP 联合训练比较。
- 只使用网络 LoRA，与网络 LoRA 加 Grid LoRA 比较。
- 矩阵 Grid LoRA与结构化 Grid LoRA 比较。
- 不同 GOP 长度比较。
- 不同 Grid rank 比较。
- 相同总参数量下不同共享模型与 LoRA 预算比例比较。

### 5.3 决策标准

满足以下条件时，将 GOP-LoRA 作为毕业论文的主要提升方向：

- 在与全视频基线相近的总参数量下，全视频 PSNR 或 MS-SSIM 有稳定提升。
- 多个视频序列上趋势一致，而不是只在 Bunny 上有效。
- 各 GOP 之间没有严重的质量失衡。
- 额外推理时间和参数切换开销可接受。

如果最终没有超过基线，则将其作为系统消融和失败分析保留，并把后续主要精力转向熵模型改进。

## 6. 阶段四 轻量跨层条件熵模型

### 6.1 研究目的

当前基础熵模型按层和通道独立预测概率，没有利用相邻位置、时间和跨尺度相关性。在高质量配置下估计码率仍然偏高。下一步应先判断问题来自量化符号本身，还是熵模型的概率预测不准确，再进行结构改进。

### 6.2 第一步 分布诊断

新增离线分析工具，输出：

- 每层 Grid 的符号直方图。
- 每层、每通道的经验熵。
- 当前熵模型的交叉熵。
- 交叉熵与经验熵之间的差值。
- 每层对总 Grid bit 的贡献比例。
- 不同量化步长下的符号范围和零值比例。

判断原则：

- 如果经验熵已经很高，应优先改进量化或加入分层步长。
- 如果经验熵较低而模型交叉熵较高，应优先改进熵模型。
- 如果高分辨率层占用大部分 bit，应优先针对高分辨率层建模。

### 6.3 轻量跨层条件模型

采用由粗到细的跨层条件建模：

1. 最粗层继续使用独立熵模型。
2. 已解码的低分辨率 Grid 通过三线性插值对齐到当前层。
3. 使用轻量通道映射预测当前层量化符号的均值和尺度。
4. 根据预测分布计算似然和估计 bit。
5. 各位置并行计算，不采用逐点自回归模型。

第一版只使用相邻粗层作为条件，暂不加入复杂上下文网络或 Transformer。

### 6.4 训练接口

- 保持现有 `train_compression.py` 可以继续运行基础熵模型。
- 新增 `--entropy_model factorized|cross_level`。
- 两种熵模型使用相同的 Grid 量化和率失真损失接口。
- 保持当前固定量化步长，第一版不同时加入量化步长调度。
- 非 Grid 混合 QAT 策略保持不变。

### 6.5 码率统计

日志继续分别报告：

- Grid 估计 bit 和逐层 bit。
- 非 Grid 参数的实际存储 bit。
- 熵模型参数 bit。
- 量化和码流元数据。
- 整体估计 BPP。

新增报告：

- 每层经验熵。
- 每层模型交叉熵。
- 跨层条件模型相对基础模型节省的 bit 和比例。

### 6.6 验收条件

- 关闭新熵模型时可以复现当前基础压缩结果。
- 新熵模型的前向、反向和 checkpoint 恢复正常。
- 相同量化符号下，新熵模型的估计 bit 低于基础模型。
- 熵模型增加的参数被计入整体 BPP。
- 重建质量下降处于可接受范围。
- 概率计算保持并行，训练和推理时间没有大幅增加。

## 7. 阶段五 真实算术编码与真实 BPP

### 7.1 实现边界

只有在阶段四的概率模型验证有效后，才接入真实编码。该阶段不再修改模型结构，重点验证估计码率与真实码流是否一致。

### 7.2 实现内容

- 将量化符号和预测分布转换为离散 CDF。
- 实现每层 Grid 的 encode 和 decode。
- 保存必要的形状、量化步长和熵模型配置。
- 验证解码后的量化 Grid 与编码前完全一致。
- 统计真实 Grid 码流、非 Grid 参数、熵模型参数和元数据。
- 输出整体真实 BPP。

### 7.3 验收条件

- 编码后能够在不读取原模型 Grid 参数的情况下完成解码。
- 解码得到的符号与编码前逐元素一致。
- 解码重建结果与量化模型直接前向一致。
- 估计 bit 与真实码流大小差距合理并可解释。
- 论文中的最终 BPP 使用真实码流统计，而不是训练损失中的 rate/value。
- 推理 FPS 与真实编解码时间分开报告。

## 8. 阶段六 完整实验与论文输出

### 8.1 数据集与指标

- Bunny 用于快速验证和主要消融。
- UVG 用于压缩率失真对比。
- DAVIS 用于重建质量、复杂运动和推理速度验证。
- 指标包括 PSNR、MS-SSIM、参数量、估计 BPP、真实 BPP、FPS、编码时间、解码时间和显存。

### 8.2 GOP 实验输出

- 每个 GOP 和全视频的 PSNR、MS-SSIM。
- 共享模型、各 GOP LoRA 和总参数量。
- GOP 长度、rank 和预算分配。
- 联合训练前、联合训练后和逐 GOP 微调后的结果。
- GOP 切换带来的额外存储和时间开销。

### 8.3 压缩实验输出

- 未压缩基线。
- 基础熵模型。
- 跨层条件熵模型。
- 是否加入非 Grid QAT。
- 估计 BPP 与真实 BPP。
- 率失真曲线和逐层 bit 分布。

### 8.4 论文表述原则

- 已完成的实验写为结果，尚未完成的内容写为计划或假设。
- 不把训练集最佳 PSNR 当作最终视频重建结果。
- 不把估计 BPP 表述为真实码率。
- 所有提升都注明比较对象、参数预算和训练条件。
- GOP-LoRA 与熵模型分别做消融，最后再讨论两者能否组合。

## 9. 推荐实现顺序

严格按照以下顺序推进：

1. 新增 `train_gop_joint.py`，完成全 GOP 联合训练最小版本。
2. 增加全 GOP checkpoint、断点恢复、`eval-only` 和参数统计。
3. 在 Bunny 上运行小轮次功能测试。
4. 在 Bunny 上完成联合训练与逐 GOP 微调正式实验。
5. 与只使用 GOP0 共享模型的现有方案比较。
6. 确认联合训练有效后，实现每 GOP rank 列表和总参数预算检查。
7. 完成固定总参数量下的 GOP-LoRA 实验。
8. 整理 GOP 方向结果并决定是否扩展到 UVG 和 DAVIS。
9. 新增 Grid 分布诊断工具，比较经验熵和模型交叉熵。
10. 实现轻量跨层条件熵模型。
11. 完成基础熵模型与跨层条件熵模型对比。
12. 接入算术编码并统计真实 BPP。
13. 完成完整对比、消融、速度评测和论文图表。
14. 核心工作完成后，再评估视频插帧或生成模型引导等下游扩展。

## 10. 当前立即执行的任务

下一项实现只做阶段一：

> 新增独立的全 GOP 联合训练入口，使共享模型在所有 GOP 上共同训练，同时为每个后续 GOP 维护独立的网络 LoRA 和结构化 Grid LoRA；支持随后冻结共享模型并逐 GOP 微调。

本次实现不加入以下内容：

- 自动 rank 分配。
- 参数预算搜索。
- 熵模型或量化。
- 算术编码。
- 多 GPU 并行。
- 新的 Grid LoRA 结构。

阶段一通过测试并完成 Bunny 小规模实验后，再决定具体正式训练参数。
